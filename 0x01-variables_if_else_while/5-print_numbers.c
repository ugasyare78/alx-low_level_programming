#include <stdio.h>

/**
  * main - Prints single digit numbers
  *
  * Return: Always 0 (Success)
  */

int main(void)
{
	int l;

	for (l = 0; l < 10; l++)
	{
		printf("%i", l);
	}

	putchar('\n');

	return (0);
}
