#include <stdio.h>

int main(void)
{
	/**
	 * main - Entry point
	 *
	 * Return: Always 0 (Success)
	 */

	char strng[] = "\"Programming is like building a multilingual puzzle";

	puts(strng);

	return (0);

}
